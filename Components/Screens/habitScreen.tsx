import React from "react";
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';


const HabitScreen = ({navigation, route}) => {
  return (
    <View>
        <Text>Here is my Habit</Text>
    </View>
  );
};

export default HabitScreen;
